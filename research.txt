Узнать, что такое terminfo, term database и чем занимается libtermcap

zfs под линукс - разнюхать, модуль для ядра + fuse, 2 способа

slack package system:
slapt-get
swaret
slacktrack

Потыкать в нестандартные просмотрщики текста, заменяющие less - может, какие тюнингованные
Потыкать zsh!!

Попробовать gvim с мордой на qt (vim-qt, есть в неком оверлее по гуглю)

горячие клавиши kde
горячие клавиши xfce

Прочекать, как работают rsync или union - из мануала по svn
На эту хрень тоже можно завести сервак, и с него раздавать неизменяемые архивы - оригиналы или ещё что
типа вместо ftp планируемого

mc курить или какой файл-менеджер

Почитать нормально про awk

Почитать про gprof

Легковесный рабочий стол на Qt

------------------------
-----  чинить ----------

Слака

Amarok mp3 не играет

Внутри Konsole коммиты закривились - когда переходит в vim для написания коммента к коммиту, картинка в Konsole не меняется - и перерисовывается только если размер окна Konsole поменять

Как загружать xfce4 через kdm либо установить какой-то оконный менеджер, который будет грузить xfce, либо сделать xdm более гламурным

Проблема:
на драйвере radeon тормозной скролл в окнах
также подглючивает перемещение qt-шных окон (хотя это менее критично)

Вот что даёт выдача:
grep DRI /var/log/Xorg.0.log
...
(EE) RADEON(0): [DRI2] DRI2SwapComplete: bad drawable

На самом деле, проблема не только в этом - общая работа драйвера зело глючная
Начальная загрузка в фреймбуфер - в полосах, при загрузке Иксов долго тупим в неработающем режиме, и потом настраиваемся на нужный режим - и не всегда причём, есть шанс того, что рухнем

Попробовать radeon без KMS ; без framebuffer

------------------------

Gentoo

Нерешенные глюки Gentoo + razorqt на amd64!

При входе в сессию razorqt:
- Не выполняется .xsetxkb (как и в xfce4 - прописать его в автостарт)
- Нет пунктов "выключить систему", только разлогиниться
- Не работает плагин для автомаунта флешек
- Не выполняется .bashrc в Konsole при логине (в true-консоли .bashrc подцеплён, в razorqt - нет)
- Сбиваются пути юзера (приходится делать source /etc/profile )
- Не сохраняются окна, открытые в прошлый раз - автостарт запилить
